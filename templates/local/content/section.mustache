{{!
    @template format_pathway/local/content/section

    Pathway format section template.
    Wraps each section in a pathway-styled container with card-based activity display.

    This extends the core section template with pathway-specific wrapper classes.
}}
{{> core_courseformat/local/content/section}}
